{
  "name": "skuli",
  "private": true,
  "scripts": {
    "dev:staff": "cd staff && npm run dev",
    "dev:student": "cd student-parent && npm run dev",
    "build:staff": "cd staff && npm run build",
    "build:student": "cd student-parent && npm run build"
  }
}
